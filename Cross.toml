# Build this target via the Haku build-aarch64 task
[target.aarch64-unknown-linux-gnu]
image = "arena-autocam/build-aarch64:latest"
runner = "native"

[target.aarch64-unknown-linux-gnu.env]
passthrough = [
  "RUST_BACKTRACE",
  "CROSS_DEBUG",
  "XDG_CACHE_HOME=/home/cross",
]
